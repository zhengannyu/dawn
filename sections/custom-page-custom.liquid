{%- style -%}{%- endstyle -%}
{% comment %} {% assign service_handle_url = request.path | split: "/" | last %} {% endcomment %}
{% assign services = shop.metaobjects.services.values %}
{% assign photos = shop.metaobjects.slider_service.values %}
<div class="custom-index-services">
  {% for service in services %}
    {% if service.system.handle == 'custom' %}
      <div class="page-width">
        {% render 'page-title', herotitle: service.herotitle %}
      </div>
      <div class="sliderimg-wrapper">
        {% for photo in photos %}
          <img
            class="slide"
            srcset="{{ photo.photo | image_url: width: 600 }}"
            sizes="(min-width: 400px) 298px, 40vw"
            alt=""
            width="auto"
            height="auto"
          >
        {% endfor %}
      </div>
      <div class="page-width">
        <div class="service-title-block">
          <h1 class="title display-1">{{ service.title }}</h1>
          <p class="line-h250 text-justify">{{ service.description }}</p>
        </div>
      </div>
      <div class="full-bg">
        <div class="page-width">
          <h1 class="display-1">ODM/OEM</h1>
          <p class="process-desp">You got a great product idea, what's next?</p>
          <div class="flex-md-row">
            <div class="w-md-50 process-left">
              <div class="process-card">
                <h5 class="number">01</h5>
                {{ images['message.svg'] | image_url: width: 60 | image_tag }}
                <div class="textarea">
                  <h4>產品方向討論</h4>
                  <p>根據您的產品需求與市場定位，深入探討並確認開發方向。</p>
                </div>
              </div>
              <div class="process-card">
                <h5 class="number">02</h5>
                {{ images['design.svg'] | image_url: width: 60 | image_tag }}
                <div class="textarea">
                  <h4>產品設計提案</h4>
                  <p>設計專屬提案，雙方確認後進行打樣，並提供樣品供審核。</p>
                </div>
              </div>
              <div class="process-card">
                <h5 class="number">03</h5>
                {{ images['sample.svg'] | image_url: width: 60 | image_tag }}
                <div class="textarea">
                  <h4>商品打樣</h4>
                  <p>根據確認的設計方案，精細打樣，確保產品符合預期與標準。</p>
                </div>
              </div>
            </div>
            <div class="w-md-50 process-right">
              <div class="process-card">
                <h5 class="number">04</h5>
                {{ images['agreement.svg'] | image_url: width: 60 | image_tag }}
                <div class="textarea">
                  <h4>產品報價</h4>
                  <p>以最終確認的樣品為基準，提供詳細報價與合作條件。</p>
                </div>
              </div>
              <div class="process-card">
                <h5 class="number">05</h5>
                {{ images['production.svg'] | image_url: width: 60 | image_tag }}
                <div class="textarea">
                  <h4>製造生產</h4>
                  <p>依據確認樣品展開規模化生產，嚴格遵循質量管控流程。</p>
                </div>
              </div>
              <div class="process-card last">
                <h5 class="number">06</h5>
                {{ images['truck.svg'] | image_url: width: 60 | image_tag }}
                <div class="textarea">
                  <h4>成品出貨</h4>
                  <p>按時完成生產，確保產品準時交付至您指定的地點。</p>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    {% endif %}
  {% endfor %}
</div>
